import { MaskType } from './mask';
import { MaskTokens } from './tokens';
export declare const parseOpts: (value: string) => boolean;
export declare const parseMask: (value: string) => MaskType;
export declare const parseTokens: (value: string) => MaskTokens;
