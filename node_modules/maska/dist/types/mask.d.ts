import { MaskTokens } from './tokens';
export declare type MaskType = string | string[] | ((input: string) => string);
export interface MaskOptions {
    mask?: MaskType;
    tokens?: MaskTokens;
    tokensReplace?: boolean;
    eager?: boolean;
    reversed?: boolean;
}
export declare class Mask {
    readonly mask: MaskType;
    readonly tokens: MaskTokens;
    readonly eager = false;
    readonly reversed = false;
    private readonly memo;
    constructor(opts?: MaskOptions);
    masked(value: string): string;
    unmasked(value: string): string;
    completed(value: string): boolean;
    private findMask;
    private escapeMask;
    private process;
}
